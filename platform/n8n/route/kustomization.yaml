# The routing a workload adds to become reachable, kept apart from the
# workload itself so that what is exposed can be read without reading the
# deployment. ADR 27.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - route.yaml
