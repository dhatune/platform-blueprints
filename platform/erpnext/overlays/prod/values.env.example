# Example only. The real file is generated by lab/bootstrap.sh from the stack's
# outputs and is not in version control, so that an environment is described in
# one place: the Terraform variables.
#
# It is committed so the overlay can be rendered and checked without building
# anything.
host=erp.prod.lab.example.test
securityPolicy=example-prod-waf
