# Example only. Generated by lab/bootstrap.sh.
#
# The application builds links from the address it believes it is on. One that
# disagrees with the route produces a login page redirecting somewhere that is
# not there.
domain=https://vault.dev.lab.example.test
smtp_host=localhost
smtp_from=vaultwarden@dev.lab.example.test
