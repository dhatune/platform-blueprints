apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vaultwarden
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
# The SMTP secret is not created here and is not in this repository. It is
# expected to exist in the cluster already, put there by whatever secret
# manager the environment uses.
